class automovel {
    nome: string;
    ano: number;

    constructor(nome: string, ano: number) {
        this.nome = nome,
        this.ano = ano
    }
}

const chevette = new automovel('chevette', 1984);